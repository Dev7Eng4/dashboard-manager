export enum ErrorMessages {
  Require = 'is required.',
  Min = 'must be at least 8 characters.',
  FormatPassword = 'Password must have at least 1 upper case and 1 digit',
}

export enum KeySessionJWT {
  AuthToken = 'AuthToken',
  AuthTokenType = 'AuthTokenType',
  AuthTokenExpire = 'AuthTokenExpireAt',
}

export enum Roles {
  Admin = 'Admin',
  Employee = 'Employee',
  User = 'User',
}

export enum MeThods {
  phone = 'phone',
  mail = 'mail',
}
